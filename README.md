# Premium Recommendation UI Flask App

Run locally:
1. python -m venv .venv
2. activate the environment
3. pip install -r requirements.txt
4. python app.py

Deploy on Render:
- Build command: pip install -r requirements.txt
- Start command: gunicorn app:app
